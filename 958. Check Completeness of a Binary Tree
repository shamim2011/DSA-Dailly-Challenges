// Approach - Level Order Traversal (BFS) using Queue
// T.c - O(n)
// S.C - O(n)



/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean isCompleteTree(TreeNode root) {

        // Queue for level order traversal
        Queue<TreeNode> q = new LinkedList<>();

        // Start BFS from root
        q.add(root);

        // Flag to indicate that a null node has been encountered
        boolean isValid = false;

        while (!q.isEmpty()) {

            // Get current node from queue
            TreeNode curr = q.poll();

            // If current node is null
            if (curr == null) {
                // Once a null is seen, all following nodes must be null
                isValid = true;
            } else {

                // If a non-null node appears after a null → not a complete tree
                if (isValid) return false;

                // Add left and right children of current node
                q.add(curr.left);
                q.add(curr.right);
            }
        }

        // If no rule was violated, tree is complete
        return true;
    }
}

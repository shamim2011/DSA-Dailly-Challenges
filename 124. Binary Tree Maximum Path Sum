//Approach 1 - Depth-First Search (DFS) – Postorder Traversal with Global Maximum Tracking
//T.C : O(n)
//S.C : O(n)


class Solution {
    // Main function to compute maximum path sum
    public int maxPathSum(TreeNode root) {

        // Using array to store global maximum (acts like pass-by-reference)
        int[] maxVal = new int[1];

        // Initialize with the smallest possible value
        maxVal[0] = Integer.MIN_VALUE;

        // Start DFS traversal
        maxPathDown(root, maxVal);

        // Final answer stored in maxVal[0]
        return maxVal[0];
    }

    // Helper function to compute maximum downward path sum
    int maxPathDown(TreeNode root, int[] maxVal) {

        // Base case
        if (root == null) return 0;

        // Recursively compute left subtree max path
        // Ignore negative paths using Math.max(0, ...)
        int leftSum = Math.max(0, maxPathDown(root.left, maxVal));

        // Recursively compute right subtree max path
        int rightSum = Math.max(0, maxPathDown(root.right, maxVal));

        // Update global maximum considering current node as highest point
        // Path may include both left and right child
        maxVal[0] = Math.max(maxVal[0], leftSum + rightSum + root.val);

        // Return max path extending downward (only one side allowed)
        return root.val + Math.max(leftSum, rightSum);
    }
}



//Approach 2 - Post-Order DFS (Maximum Path Sum in Binary Tree)
//T.C : O(n)
//S.C : O(h)


/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    // Global variable to store the maximum path sum found so far
    int maxVal;

    public int maxPathSum(TreeNode root) {
        // Initialize with minimum value to handle negative nodes
        maxVal = Integer.MIN_VALUE;

        // Start post-order traversal
        maxPathDown(root);

        // Final answer stored in maxVal
        return maxVal;
    }
    
    // Returns maximum path sum starting from current node and going DOWNWARD
    int maxPathDown(TreeNode root) {

        // Base case: null node contributes 0
        if (root == null) return 0;

        // Recursively get max path sum from left subtree
        int leftSum = maxPathDown(root.left);

        // Recursively get max path sum from right subtree
        int rightSum = maxPathDown(root.right);

        // Case 1: Path passes through current node and both children (U-turn)
        int OnlyUturn = leftSum + rightSum + root.val;

        // Case 2: Path includes current node and either left or right subtree
        int anyOneLorR = Math.max(leftSum, rightSum) + root.val;

        // Case 3: Path consists of only the current node
        int onlyRoot = root.val;

        // Update global maximum with best of all cases
        maxVal = Math.max(
                    maxVal,
                    Math.max(OnlyUturn, Math.max(anyOneLorR, onlyRoot))
                 );

        // Return the maximum downward path sum to parent
        return Math.max(anyOneLorR, onlyRoot);
    }
}
